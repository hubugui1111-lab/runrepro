name: Python dependency failure
on: workflow_dispatch
permissions:
  contents: read
jobs:
  reproduce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Import undeclared Python package
        run: python -c "import runrepro_demo_missing"
